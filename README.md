# MSME-Financial-Chatbot-Personalization-System
Use unsupervised machine learning (Clustering + PCA) to segment Kenyan MSMEs, then deliver highly personalized financial advice using a local LLM (Ollama).
